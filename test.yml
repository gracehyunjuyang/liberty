- hosts: all
  gather_facts: no
  tasks:

  - name: server start
   # shell: /was/AppServer/wlp/bin/server start {{ server_name }}
     shell: ssh root@169.56.98.12 '/was/AppServer/wlp/bin/server start "{{ server_name }}"'
     delegate_to: localhost
 # - name: Start
 #   liberty_server:
 #     state: started
 #     libertydir: /was/AppServer/wlp
 #     name: "{{ server_name }}"
